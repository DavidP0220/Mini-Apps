---
description: Contexto y estado de Human Chronicles sin gastar tokens de más
---

Trabajo sobre el canal **Human Chronicles**. Antes de responder o hacer nada:

1. Lee `human-chronicles/SINTESIS.md` entero (es barato, ~3.000 tokens, y da el cuadro completo).
2. Ejecuta `node human-chronicles/tools/hc.mjs estado` para la ficha actual del canal.
3. Si vas a producir, guionizar, publicar o gastar créditos, lee además
   `human-chronicles/documentos_canal/ERRORES_A_EVITAR.md` **entero** con `cat` — es obligatorio
   antes de cualquier trabajo del canal y el hook lo bloquea con Read a propósito.

**No leas ningún otro documento entero.** Usa `node human-chronicles/tools/hc.mjs ver <doc> <n>`
para traer solo la sección que necesites.

Reglas que no se negocian en este canal:
- Comunicación conmigo **siempre en español**; el material del canal, **siempre en inglés**.
- **Ni un crédito** de Recraft/VideoExpress sin autorización explícita de David para ese lote.
- **Nada se publica** sin el visto de David viendo el render, no una descripción.
- **Ejecutar primero, documentar después.** Verifica en disco antes de afirmar que algo existe.
- Solo `human-chronicles-program-director` escribe en `TABLERO_MONETIZACION.md`.

$ARGUMENTS
